# Capability map entry point

The former broad task map has been replaced by the source-linked [pre-query overview](catalog/overview.md). Read that overview and the relevant domain/feature cards before any API database query or Blender-specific implementation.

The [evidence workflow](evidence-workflow.md) defines the requirement to reject pretrained Blender knowledge as evidence, preserve source gaps, record the passages actually read, and bind task code to those passages before execution.

This file is retained as a compatibility link for earlier references; it contains no independent feature advice.
