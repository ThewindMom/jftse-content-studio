# Geometry Nodes, fields and instances

Select by purpose and evidence below. Identify a route/source card before querying SQLite or proposing API identifiers.

## Geometry Nodes graph and modifier — `geometry-nodes-graph`

GeometryNodeTree holds linked geometry nodes; NodesModifier.node_group references the graph driving the modifier.

Choose when: Create a geometry graph or a modifier driven by a node group; select specific node routes for processing operations.

Search aliases: dựng Geometry Nodes, hình học procedural, node graph hình học, geometry node group, nodes modifier, procedural geometry graph

Evidence: `bpy.types.GeometryNodeTree.html` — A node tree contains linked nodes for geometry.; `bpy.types.NodesModifier.html#bpy.types.NodesModifier.node_group` — A node group controls the modifier.; `bpy.types.Nodes.html#bpy.types.Nodes.new` — Create a node using bl_idname; the source warns against node.type.; `bpy.types.NodeTreeInterface.html#bpy.types.NodeTreeInterface.new_socket` — Create an interface socket; socket_type is listed as DEFAULT.

Read next: `bpy.types.GeometryNodeTree.html`, `bpy.types.NodesModifier.html#bpy.types.NodesModifier.node_group`, `bpy.types.Nodes.html#bpy.types.Nodes.new`, `bpy.types.NodeLinks.html#bpy.types.NodeLinks.new`, `bpy.types.NodeTreeInterface.html#bpy.types.NodeTreeInterface.new_socket`

Limits: Nodes.new requires node.bl_idname, not node.type. NodeTreeInterface.new_socket lists only DEFAULT for socket_type; this page does not establish all runtime socket types.

## Surface point distribution and instancing — `geometry-nodes-surface-scatter`

DistributePointsOnFaces generates points on mesh surfaces; InstanceOnPoints creates geometry references at points without duplicating underlying data.

Choose when: Distribute geometry instances over a surface; trees or rocks describe user-supplied content, not assets generated by these nodes.

Search aliases: rải cây, rải đá, phân bố vật thể trên mặt, scatter objects, surface scattering, distribute points on faces, instance on points

Evidence: `bpy.types.GeometryNodeDistributePointsOnFaces.html` — Generate points scattered on a mesh surface with RANDOM or POISSON distribution.; `bpy.types.GeometryNodeInstanceOnPoints.html` — Generate geometry references at each point without duplicating underlying data.

Read next: `bpy.types.GeometryNodeDistributePointsOnFaces.html`, `bpy.types.GeometryNodeInstanceOnPoints.html`, `bpy.types.GeometryNodeTree.html`

Limits: These nodes do not supply tree or rock models; they describe point distribution and geometry references.

## Realize instances — `geometry-nodes-realize`

RealizeInstances converts instances into real geometry data.

Choose when: The input contains instances and a later step requires geometry data instead of instance references.

Search aliases: chuyển instance thành hình học thật, hiện thực instance, realize instances, instances to geometry, real geometry data

Evidence: `bpy.types.GeometryNodeRealizeInstances.html` — Convert instances to real geometry data; describe realize_to_point_domain as legacy compatibility.

Read next: `bpy.types.GeometryNodeRealizeInstances.html`, `bpy.types.GeometryNodeInstanceOnPoints.html`

Limits: The source says geometry data; it does not claim every input type becomes a mesh. realize_to_point_domain is documented as compatibility behavior for version 5.0 and earlier.

## Named attribute field storage and retrieval — `geometry-nodes-attribute-fields`

StoreNamedAttribute stores a field result on geometry under a name; InputNamedAttribute reads the specified attribute.

Choose when: Exchange geometry data through named attributes in a node graph, with source-supported types and domains.

Search aliases: ghi named attribute, đọc thuộc tính node, lưu field, store named attribute, named attribute, field to attribute

Evidence: `bpy.types.GeometryNodeStoreNamedAttribute.html` — Store a field result under a name with data_type and domain.; `bpy.types.GeometryNodeInputNamedAttribute.html` — Retrieve a specified attribute with data_type.

Read next: `bpy.types.GeometryNodeStoreNamedAttribute.html`, `bpy.types.GeometryNodeInputNamedAttribute.html`, `bpy.types.Attribute.html`

## Geometry raycast hit information — `geometry-nodes-raycast`

GeometryNodeRaycast casts rays from context geometry toward target geometry and retrieves hit information.

Choose when: Compute ray/geometry hit information in a Geometry Nodes graph.

Search aliases: raycast trong node, bắn tia vào mesh, lấy điểm chạm bề mặt, geometry raycast, hit point data, ray target geometry

Evidence: `bpy.types.GeometryNodeRaycast.html` — Cast rays from context geometry to target geometry and retrieve hit-point information.

Read next: `bpy.types.GeometryNodeRaycast.html`, `bpy.types.GeometryNodeTree.html`

Limits: This describes a geometry query, not renderer shadow configuration.

## Simulation zone input output pairing — `geometry-nodes-simulation-zone`

SimulationInput/Output pass data into and out of a simulation zone; pair_with_output pairs the nodes and reports success.

Choose when: A simulation zone is required and the task needs the node pairing and state-data APIs.

Search aliases: simulation zone, vùng mô phỏng Geometry Nodes, ghép simulation input output, node simulation state

Evidence: `bpy.types.GeometryNodeSimulationInput.html` — Describe simulation zone input and paired_output.; `bpy.types.GeometryNodeSimulationOutput.html` — Describe simulation zone output and state_items.; `bpy.types.GeometryNodeSimulationInput.html#bpy.types.GeometryNodeSimulationInput.pair_with_output` — Pair a zone input with an output and return a success boolean.

Read next: `bpy.types.GeometryNodeSimulationInput.html`, `bpy.types.GeometryNodeSimulationOutput.html`, `bpy.types.NodesModifier.html`

Limits: These pages describe node pairing and state items; they do not provide simulation equations or a complete evaluation/baking workflow for a particular effect.

## Discover all source pages in this domain

`python3 scripts/features.py pages geometry-nodes` lists 510 source cards with pagination. Each card retains its source name and description excerpt. Catalog discovery does not require SQLite.

Source pages remain discoverable even without an authored route. Read their descriptions/definitions before planning; do not supply missing behavior from model knowledge.
